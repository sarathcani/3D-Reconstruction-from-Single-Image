#### Renderer script for blender

import bpy

def main() :
	### Loop through the 3D dataset
	### import model one by one
	### place the camera at 24 different positions and render the images as `PNG`

if __name__ == '__main__':
	main()